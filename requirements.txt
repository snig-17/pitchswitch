# Runtime deps for the hosted app (serves the pre-built broadcast cache).
# Lightweight so Streamlit Community Cloud installs fast and reliably.
streamlit>=1.45.0
pandas>=2.0.0
requests>=2.31.0
python-dotenv>=1.0.0
