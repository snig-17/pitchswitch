__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.env
.venv/
venv/
*.egg
.pytest_cache/
data/*.json
.gstack/

# Metrica tracking CSVs (large; download via scripts/get_metrica.sh)
data/metrica/*.csv

# Cached pre-built broadcasts
# data/cache/ is committed: pre-built self-contained broadcasts so the
# deployed app runs without Ollama/Metrica data. Rebuild with
# scripts/prebuild_cache.py.
